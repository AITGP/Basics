--DESPUES DE COPIAR TODOS LOS ARCHIVOS REINICIAR EL SERVIDOR
--EL PROCEDIMIENTO PLSQL DEBE TENER LA SIGUIENTE ESTRUCTURA

 procedure <PROCEDURE NAME>(<PARAMETERS>) IS 
    begin
     open emp_cursor for
     lv_select||lv_from||lv_where; 
    end;

--EL QUERY DEL REPORTE VA ASI 
{call <PROCEDURE NAME>(<PARAMETERS>)}


--CUANDO SE PONE EL QUERY, VA A SALIR UN ERROR 
--Y HAY QUE AGREGAR EL TOTAL DE FIELDS, MAXIMOS QUE EL QUERY VA A RETORNAR 


-----------------------------------------------------------------------------------

- AFTER COPYING ALL THE FILES, RESTART THE SERVER
--THE PLSQL PROCEDURE MUST HAVE THE FOLLOWING STRUCTURE

 procedure <PROCEDURE NAME>(<PARAMETERS>) IS 
    begin
     open emp_cursor for
     lv_select||lv_from||lv_where; 
    end;

--THE QUERY OF THE REPORT GOES LIKE THIS
{call <PROCEDURE NAME>(<PARAMETERS>)}

- WHEN THE QUERY IS SET, AN ERROR WILL GET
- AND YOU HAVE TO ADD THE TOTAL OF FIELDS, MAXIMUM THAT THE WANT IS GOING TO RETURN

